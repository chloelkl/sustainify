{
  "name": "sustainify",
  "version": "1.0.0",
  "scripts": {
    "start": "node server/index.js",
    "heroku-postbuild": "cd client && npm ci && npm run build && cd ../server && npm ci"
  },
  "dependencies": {
    "express": "^4.17.1"
  },
  "engines": {
    "node": "20.x",
    "npm": "10.x"
  }
}
